%h3
  Posts

.post
  %h4
    =@post.title
  %p
    This will be the imbeded url: 
    =@post.video_url
  %p
    =@post.content
  = link_to "Edit", edit_post_path(@post)
  = link_to "Delete", post_path(@post), :method => :delete
  = link_to "Comment"

  
  - @post.comments.each do |comment|
    %h5
      =comment.title
    %p
      =comment.comment
    %p
      =link_to comment.user.name, user_path(comment.user)